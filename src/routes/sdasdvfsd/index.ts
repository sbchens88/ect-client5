import { Router } from 'express';
import mountFDFSDFDSF from './fdfsdfdsf';

export default function mountSDASDVFSD(router: Router) {
    const fdfsdfdsf = Router();
    mountFDFSDFDSF(fdfsdfdsf);
    router.use('/fdfsdfdsf', fdfsdfdsf);
}
