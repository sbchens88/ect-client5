import { Router } from 'express';
import mountDASDASDAS from './dasdasdas';

export default function mountASDSA(router: Router) {
    const dasdasdas = Router();
    mountDASDASDAS(dasdasdas);
    router.use('/dasdasdas', dasdasdas);
}
